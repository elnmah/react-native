import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    ex: {
        paddingHorizontal:14,
        marginVertical:5,
        borderRadius: 10,
        borderWidth:1,
        borderColor: '#222',
        fontSize: 24,
        fontWeight:'bold'
    }
})